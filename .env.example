# OAuth2 Configuration
# Required: OAuth2 provider authority URL (base URL for the OAuth provider)
VITE_OAUTH_AUTHORITY=https://your-provider.com

# Required: OAuth2 client ID registered with the provider
VITE_OAUTH_CLIENT_ID=your-client-id

# Required: OAuth2 scopes to request (space-separated)
VITE_OAUTH_SCOPE=openid profile email

# Optional: Redirect URI after authentication (defaults to current origin)
# VITE_REDIRECT_URI=http://localhost:3000

# Example configurations:
# Auth0:
# VITE_OAUTH_AUTHORITY=https://your-tenant.auth0.com
# VITE_OAUTH_CLIENT_ID=your-auth0-client-id
# VITE_OAUTH_SCOPE=openid profile email

# GitHub (note: GitHub doesn't support OIDC discovery, would need custom implementation):
# VITE_OAUTH_AUTHORITY=https://github.com
# VITE_OAUTH_CLIENT_ID=your-github-client-id
# VITE_OAUTH_SCOPE=user:email

# Google:
# VITE_OAUTH_AUTHORITY=https://accounts.google.com
# VITE_OAUTH_CLIENT_ID=your-google-client-id.googleusercontent.com
# VITE_OAUTH_SCOPE=openid profile email